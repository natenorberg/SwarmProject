package softcomputing.project4.enums;

/**
 * Enum that holds the condition that terminates the loop
 */
public enum StopCondition
{
    Iterations, // Stop after a given number of iterations
    Convergence // Stop when the algorithm converges
}
